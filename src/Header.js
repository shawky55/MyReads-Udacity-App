const Header = ({header}) => {
    return (
        <>
            <div className="list-books-title">
                <h1>{header}</h1>
            </div>
        </>
    );
};

export default Header;
